version: '3.8'
services:
  postgres:
    image: postgres:13
    container_name: postgres_liquibase
    environment:
      POSTGRES_PASSWORD: "1234"
      POSTGRES_DB: tienda_db
      POSTGRES_USER: postgres
    ports:
      - "5434:5432"